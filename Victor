<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <!-- Google Font: Maven Pro (change to another Google font if you prefer) -->
  <link href="https://fonts.googleapis.com/css2?family=Maven+Pro:wght@400;600;700&display=swap" rel="stylesheet">
  <title>Styled Link</title>
  <style>
    :root{
      --brand-red: #e60000;   /* background red (change if you want a different red) */
      --white: #ffffff;
      --radius: 8px;
      --padding-x: 20px;
      --padding-y: 12px;
      --font-stack: 'Maven Pro', system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }

    body{
      font-family: var(--font-stack);
      display:flex;
      align-items:center;
      justify-content:center;
      min-height:100vh;
      margin:0;
      background:#fff;
    }

    .brand-link {
      display:inline-flex;
      align-items:center;
      gap:10px;
      text-decoration:none;
      background: linear-gradient(180deg, var(--brand-red) 0%, #c40000 100%);
      color: var(--white);
      padding: var(--padding-y) var(--padding-x);
      border-radius: var(--radius);
      font-weight: 600;
      box-shadow: 0 6px 18px rgba(230,0,0,0.18);
      transition: transform .08s ease, box-shadow .12s ease, filter .12s ease;
      border: 2px solid rgba(255,255,255,0.08);
    }

    .brand-link:hover{
      transform: translateY(-2px);
      box-shadow: 0 10px 26px rgba(230,0,0,0.22);
      filter: brightness(.98);
    }

    .brand-link:active{ transform: translateY(0); }

    /* small icon (optional) */
    .brand-link .icon{
      width:20px;
      height:20px;
      display:inline-block;
      border-radius:4px;
      background: rgba(255,255,255,0.18);
      display:flex;
      align-items:center;
      justify-content:center;
      font-size:12px;
      color:var(--white);
      opacity:.95;
    }
  </style>
</head>
<body>

  <!-- Replace href with any target URL you want -->
  <a class="brand-link" href="https://ffbb01.cc/reg.html?v=8KTNFW" target="_blank" rel="noopener noreferrer">
    <span class="icon">→</span>
    <span>Register Now</span>
  </a>

</body>
</html>
